-- Activar el esquema que vamos a usar


USE asxbd;

-- Si ya existen las tablas, eliminarlas en orden correcto
DROP TABLE IF EXISTS valoraciones;
DROP TABLE IF EXISTS stock;
DROP TABLE IF EXISTS proveedores;
DROP TABLE IF EXISTS detalle_pedido;
DROP TABLE IF EXISTS pedidos;
DROP TABLE IF EXISTS productos;
DROP TABLE IF EXISTS categorias;
DROP TABLE IF EXISTS clientes;

-- TABLAS PRINCIPALES

CREATE TABLE clientes (
    id_cliente INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(50) NOT NULL,
    apellidos VARCHAR(80),
    email VARCHAR(100) UNIQUE,
    telefono VARCHAR(20),
    fecha_registro TIMESTAMP  DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE categorias (
    id_categoria INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(50) NOT NULL
);

CREATE TABLE productos (
    id_producto INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(80) NOT NULL,
    precio DECIMAL(8,2) NOT NULL,
    id_categoria INT,
    FOREIGN KEY (id_categoria) REFERENCES categorias(id_categoria)
);

CREATE TABLE pedidos (
    id_pedido INT AUTO_INCREMENT PRIMARY KEY,
    id_cliente INT NOT NULL,
    fecha_pedido DATE NOT NULL,
    total DECIMAL(10,2),
    FOREIGN KEY (id_cliente) REFERENCES clientes(id_cliente)
);

CREATE TABLE detalle_pedido (
    id_detalle INT AUTO_INCREMENT PRIMARY KEY,
    id_pedido INT NOT NULL,
    id_producto INT NOT NULL,
    cantidad INT NOT NULL,
    subtotal DECIMAL(10,2),
    FOREIGN KEY (id_pedido) REFERENCES pedidos(id_pedido),
    FOREIGN KEY (id_producto) REFERENCES productos(id_producto)
);


-- Tablas ampliadas
CREATE TABLE proveedores (
    id_proveedor INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    telefono VARCHAR(20),
    ciudad VARCHAR(50)
);

CREATE TABLE stock (
    id_stock INT AUTO_INCREMENT PRIMARY KEY,
    id_producto INT NOT NULL,
    id_proveedor INT NOT NULL,
    unidades INT NOT NULL,
    FOREIGN KEY (id_producto) REFERENCES productos(id_producto),
    FOREIGN KEY (id_proveedor) REFERENCES proveedores(id_proveedor)
);

CREATE TABLE valoraciones (
    id_valoracion INT AUTO_INCREMENT PRIMARY KEY,
    id_cliente INT NOT NULL,
    id_producto INT NOT NULL,
    puntuacion INT CHECK (puntuacion BETWEEN 1 AND 5),
    comentario VARCHAR(255),
    fecha DATE,
    FOREIGN KEY (id_cliente) REFERENCES clientes(id_cliente),
    FOREIGN KEY (id_producto) REFERENCES productos(id_producto)
);


INSERT INTO clientes (nombre, apellidos, email, telefono) VALUES
('Laura', 'Martínez Ruiz', 'laura@example.com', '600123123'),
('Pablo', 'Gómez Díaz', 'pablo@example.com', '611456456'),
('María', 'López Varela', 'maria@example.com', '622789789'),
('Carlos', 'Souto Vila', 'carlos@example.com', '634888999');

INSERT INTO categorias (nombre) VALUES
('Electrónica'), ('Hogar'), ('Juguetes'), ('Ropa');

INSERT INTO productos (nombre, precio, id_categoria) VALUES
('Televisor 55"', 599.99, 1),
('Portátil 14"', 750.00, 1),
('Aspiradora Pro', 129.50, 2),
('Plancha Vapor', 49.90, 2),
('Muñeca articulada', 19.90, 3),
('Coche teledirigido', 29.90, 3),
('Camiseta básica', 12.00, 4),
('Sudadera premium', 39.50, 4);

INSERT INTO pedidos (id_cliente, fecha_pedido, total) VALUES
(1, '2024-01-15', 619.89),
(2, '2024-01-20', 142.00),
(3, '2024-01-22', 750.00);

INSERT INTO detalle_pedido (id_pedido, id_producto, cantidad, subtotal) VALUES
(1, 1, 1, 599.99),
(1, 5, 1, 19.90),
(2, 3, 1, 129.50),
(2, 7, 1, 12.00),
(3, 2, 1, 750.00);

INSERT INTO proveedores (nombre, telefono, ciudad) VALUES
('TechSupplier', '988445566', 'Madrid'),
('HomeImport', '933112233', 'Barcelona'),
('ToyWorld', '945778899', 'Valencia');

INSERT INTO stock (id_producto, id_proveedor, unidades) VALUES
(1, 1, 50),
(2, 1, 40),
(3, 2, 100),
(4, 2, 80),
(5, 3, 200),
(6, 3, 150);

INSERT INTO valoraciones (id_cliente, id_producto, puntuacion, comentario, fecha) VALUES
(1, 1, 5, 'Muy buena calidad', '2024-02-01'),
(2, 3, 4, 'Funciona bien', '2024-02-05'),
(3, 5, 3, 'Aceptable', '2024-02-10'),
(4, 2, 5, 'Excelente portátil', '2024-02-12');

select * from clientes;

-- truncate  table detalle_pedido;


SELECT p.nombre, p.precio
FROM productos p
JOIN categorias c ON p.id_categoria = c.id_categoria
WHERE c.nombre = 'Electrónica';
